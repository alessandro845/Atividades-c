#include <stdio.h>

int main()
{  
     
    int v1;
    int s1;
    int v2;
    int s2;
    int soma;
    
    
    printf("informe o valor do primeiro vetor: ");
    scanf("%d",&v1);
    
    printf("informe o sentido do primeiro vetor,seguindo a regra abaixo: ");
    printf("\n1 para o sentido ESQUERDA-DIREITA// -1 para o sentido DIREITA-ESQUERDA");
    scanf("%d",&s1);
    
        
    printf("informe o valor do primeiro vetor: ");
    scanf("%d",&v1);
    
    printf("informe o sentido do primeiro vetor,seguindo a regra abaixo: ");
    printf("\n1 para o sentido ESQUERDA-DIREITA// -1 para o sentido DIREITA-ESQUERDA");
    scanf("%d",&s1);

    soma = v1*s1 + v2*s2;
    
    printf("o resultado da soma é %d",soma);
    
    return 0;
